general:
  build_dir: tests
machine:
  post:
    - pyenv global 3.5.1
dependencies:
  pre:
    - sudo apt-get install poppler-utils
  post:
    - pip install pillow
    - pip install psutil
    - pip install memory_profiler
test:
  override:
    - python3 tests.py
